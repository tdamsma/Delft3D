[project]
name = "ciutilities"
version = "0.1.0"
description = "An environment to manage CI tools and their versions"
requires-python = ">=3.11"
dependencies = [
    "dvc ~= 3.0",
    "dvc-s3 ~= 3.0",
    "jinja2-cli>=1.0.0",
]
