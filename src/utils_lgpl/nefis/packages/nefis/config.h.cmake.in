/* Required definitions for building deltares common
   */

#include "FortranCInterface.h"

#if defined(__APPLE__) && !defined(FC_GLOBAL)
    #define FC_GLOBAL(name, NAME) name ## _
#endif
#define FC_FUNC FC_GLOBAL
